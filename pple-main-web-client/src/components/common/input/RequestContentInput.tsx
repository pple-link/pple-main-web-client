import React from 'react';
import { styled } from '@mui/material';

const RequestContentInput: React.FC = () => {
    return (
        <div>
            
        </div>
    );
};

export default RequestContentInput;